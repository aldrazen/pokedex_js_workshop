<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include("../partials/header") %>
  </head>
  <body class="bg-gray-100 text-gray-800">
    <div class="container mx-auto p-5">
      <div class="flex flex-col items-center">
        <img src="<%= pokemon.sprites.versions['generation-v']['black-white'].animated.front_default || pokemon.sprites.front_default %>" alt="<%= pokemon.name %>" class="pokemon_image" />
        <h1 class="text-2xl font-bold text-center mb-5 capitalize pokemon_data">
          <span><%= pokemon.id %> - </span>
          <span><%= pokemon.name %></span>
        </h1>
        <div class="more_details p-5 border-2 font-bold rounded-lg">
          <p>Height: <%= pokemon.height %></p>
          <p>Weight: <%= pokemon.weight %></p>
          <p>Type: <%= pokemon.types.map(t => t.type.name).join(', ') %></p>
        </div>

        <img src="/images/pokedexm.png" alt="pokedex" class="pokedex" />
      </div>
      <div class="text-center mt-5 button">
        <a href="/" class="px-4 py-2 bg-gray-500 text-white rounded-lg border-2 border-black hover:shadow-xl">Back</a>
      </div>
    </div>
    <%- include("../partials/footer") %>
  </body>
</html>
